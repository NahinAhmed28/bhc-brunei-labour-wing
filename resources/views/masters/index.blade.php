@extends('layouts.app') @section('title',$title) @section('content')<div class="d-flex justify-content-between align-items-end mb-4"><div><div class="page-eyebrow">Master data</div><h1 class="page-title">{{ $title }}</h1><p class="page-lead mb-0">Controlled records used by every new token.</p></div><a class="btn btn-primary" href="{{ route($type.'.create') }}"><i class="bi bi-plus-lg me-2"></i>Add {{ $type==='companies'?'company':'agency' }}</a></div><div class="card"><div class="card-body border-bottom"><form class="row g-2"><div class="col-md-5"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Search by title"></div><div class="col-auto"><button class="btn btn-outline-primary">Search</button></div></form></div><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th class="ps-4">Title</th><th>Email</th><th>Phone</th><th>Status</th><th class="text-end pe-4">Actions</th></tr></thead><tbody>@forelse($items as $item)<tr><td class="ps-4"><strong>{{ $item->name }}</strong><div class="small text-secondary">{{ $item->registration_no ?? $item->license_no ?? 'No registration reference' }}</div></td><td>{{ $item->email ?: '—' }}</td><td>{{ $item->phone ?: '—' }}</td><td><span class="status {{ $item->is_active?'status-success':'status-danger' }}">{{ $item->is_active?'Active':'Inactive' }}</span></td><td class="text-end pe-4"><a class="btn btn-sm btn-light" href="{{ route($type.'.edit',$item) }}">Edit</a>@if(auth()->user()->isSuperAdmin())<form class="d-inline" method="post" action="{{ route($type.'.destroy',$item) }}" data-confirm="Deactivate this record?">@csrf @method('delete')<button class="btn btn-sm btn-link text-danger">Deactivate</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-5">No records found.</td></tr>@endforelse</tbody></table></div>@if($items->hasPages())<div class="card-footer bg-white">{{ $items->links() }}</div>@endif</div>@endsection
