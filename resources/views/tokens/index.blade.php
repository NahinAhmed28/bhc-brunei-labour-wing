@extends('layouts.app') @section('title','Token Submissions') @section('content')<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><div class="page-eyebrow">Token operations</div><h1 class="page-title">Token Submissions</h1><p class="page-lead mb-0">{{ number_format($tokens->total()) }} total tokens · {{ number_format(\App\Models\Token::where('pre_selected',true)->count()) }} pre applicant tokens</p></div><a class="btn btn-primary" href="{{ route('tokens.create') }}"><i class="bi bi-plus-lg me-2"></i>Create Token</a></div><div class="card"><div class="card-body border-bottom"><form class="row g-2"><div class="col-lg-3"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Token, BHC, company or agency"></div><div class="col-lg-2"><select class="form-select" name="company_id"><option value="">All companies</option>@foreach($companies as $x)<option value="{{ $x->id }}" @selected(request('company_id')==$x->id)>{{ $x->name }}</option>@endforeach</select></div><div class="col-lg-2"><select class="form-select" name="agency_id"><option value="">All agencies</option>@foreach($agencies as $x)<option value="{{ $x->id }}" @selected(request('agency_id')==$x->id)>{{ $x->name }}</option>@endforeach</select></div><div class="col-lg-2"><select class="form-select" name="desk_id"><option value="">All desks</option>@foreach($desks as $x)<option value="{{ $x->id }}" @selected(request('desk_id')==$x->id)>{{ $x->name }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Show Filters</button></div><div class="col-auto"><a class="btn btn-light" href="{{ route('tokens.index') }}">Clear</a></div></form></div><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th class="ps-4">Token</th><th>Token Category</th><th>Agency</th><th>Company</th><th>Pre Selected Applicant</th><th>Received On</th><th>Demanded Workers / Visa Attestation</th><th>Approved Workers</th><th>BHC No.</th><th>Send to BOESL</th><th class="pe-4">Actions</th></tr></thead><tbody>@forelse($tokens as $token)<tr><td class="ps-4"><a class="fw-bold text-decoration-none" href="{{ route('tokens.show',$token) }}">{{ $token->token_number }}</a><div class="small text-secondary">{{ $token->applicants_count }} applicants</div></td><td>{{ $token->category->name }}</td><td>{{ $token->agency->name }}</td><td>{{ $token->company->name }}</td><td>{{ $token->pre_selected?'Yes':'No' }}</td><td>{{ $token->received_on->format('d M Y') }}</td><td>{{ $token->demanded_workers }}</td><td>{{ $token->approved_workers }}</td><td>{{ $token->bhc_number ?: '—' }}</td><td><span class="status {{ $token->boesl_status==='submitted'?'status-success':'status-warning' }}">{{ ucfirst($token->boesl_status) }}</span></td><td class="text-nowrap pe-4"><a class="btn btn-sm btn-light" href="{{ route('tokens.show',$token) }}">View Details</a>@if(auth()->user()->hasAnyRole('super-admin','administrator'))<a class="btn btn-sm btn-light" href="{{ route('tokens.edit',$token) }}">Edit</a>@endif<a class="btn btn-sm btn-outline-primary" href="{{ route('tokens.pdf',$token) }}">PDF</a></td></tr>@empty<tr><td colspan="11" class="text-center text-secondary py-5">No token submissions match these filters.</td></tr>@endforelse</tbody></table></div>@if($tokens->hasPages())<div class="card-footer bg-white">{{ $tokens->links() }}</div>@endif</div>@endsection
